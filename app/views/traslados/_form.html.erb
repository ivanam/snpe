<%= form_for(@traslado, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @traslado.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4>No se pudo guardar el formulario, tiene los siguientes errores:</h4>

      <ul>
      <% @traslado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :alta_baja_hora_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :alta_baja_hora_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cargo_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :cargo_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cargo_no_docente_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :cargo_no_docente_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :fecha_cambio_oficina, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.date_select :fecha_cambio_oficina, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :user_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :user_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">

      <%= f.submit :class => "btn btn-primary", :value => "Guardar" %>
      <%= link_to traslados_path, :class => 'btn btn-default' do %>
        <span class="glyphicon glyphicon-list-alt"></span>
        Atrás
      <% end %>
    </div>
  </div>
<% end %>
