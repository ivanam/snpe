<div class="page-header">
  <%= link_to new_plan_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Plan
  <% end %>
  <h1>Listado de planes</h1>
</div>

<div class="table-responsive">
  <table id="tabla-plan" class="table table-striped table-bordered table-hover" data-source = "<%= plans_path(format: :json) %>" >
    <thead>
      <tr>
            <th>Código</th>
            <th>Descripción</th>
            <th class="text-center col-md-1">Acciones</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>


<script type="text/javascript">
    $(document).ready(function($) {
      $('#tabla-plan').dataTable({
        language : {"sUrl": "<%= asset_path('language.es.json') %>",},
        processing : true,
        serverSide : true,
        ajax : $('#tabla-plan').data('source'),
        pagingType: "simple",
      });
    });
</script>