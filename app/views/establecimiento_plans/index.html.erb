<div class="page-header">
  <%= link_to new_establecimiento_plan_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Establecimiento plan
  <% end %>
  <h1>Listado de establecimiento_plans</h1>
</div>

<div class="table-responsive">
  <table id="tabla-establecimiento_plan" class="table table-striped table-bordered table-hover" data-source = "<%= establecimiento_plans_path(format: :json) %>" >
    <thead>
      <tr>
            <th>Establecimiento</th>
            <th>Plan</th>
            <th class="text-center col-md-1">Acciones</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>


<script type="text/javascript">
    $(document).ready(function($) {
      $('#tabla-establecimiento_plan').dataTable({
        language : {"sUrl": "<%= asset_path('language.es.json') %>",},
        processing : true,
        serverSide : true,
        ajax : $('#tabla-establecimiento_plan').data('source'),
        pagingType: "simple",
      });
    });
</script>