<ul class="nav navbar-nav">
  <% if user_signed_in? then %>
    <% if (current_user.role? :sadmin) || (current_user.role? :personal)  then %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Licencias <b class="caret"></b></a>
        <ul class="dropdown-menu">        
          <li><%=link_to "Licencias", licencia_path, :tabindex => "-1"%></li>
          <li><%=link_to "Listado de licencias", listado_licencias_path, :tabindex => "-1"%></li>          
        </ul>
      </li>
    <% end %>
  <% end %>
</ul>      