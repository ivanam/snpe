<ul class="nav navbar-nav">
  <% if user_signed_in? then %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Licencias <b class="caret"></b></a>
        <ul class="dropdown-menu">        
          <li><%=link_to "Licencias", licencia_path, :tabindex => "-1"%></li>
          <li><%=link_to "Parte diario de licencias", listado_licencias_path, :tabindex => "-1"%></li>
          <li><%=link_to "Historial Licencias", historial_agente_activo_path, :tabindex => "-1"%></li>
          <% if (user_signed_in? && ((current_user.role? :licencia) || (current_user.role? :sadmin))) %>
            <li><%=link_to "Listado de licencias sin goce de haberes", listado_licencias_todas_lic_path, :tabindex => "-1"%></li>
          <%end%>
          <li><%=link_to "Lic. sin goce CANCELADAS", licencias_sin_goce_canceladas_path, :tabindex => "-1"%></li>
        </ul>
      </li>
  <% end %>
</ul>      