<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Sistema de Novedades de Personal de Educación</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="description" content="">
  <meta name="author" content="">

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <%= javascript_include_tag "https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js", "https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js" %>
  <![endif]-->

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %> 
  <%= csrf_meta_tags %>
  <%= javascript_include_tag :defaults, 'nested_form' %>
  <%= stylesheet_link_tag    "bootstrap-editable" %>
  <%= javascript_include_tag "combodate"%>
  <%= javascript_include_tag "moment-with-locales"%> 

</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "SNPE", root_path, :class => "navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <% if user_signed_in? then %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-home"></span><% Establecimiento.where(:id => session[:establecimiento]).first() %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Establecimientos", establecimientos_de_usuario_path,  :tabindex => "-1" %></li>
              </ul>
            </li>            
          <% end %>
        </ul>
        
        <ul class="nav navbar-nav">
          <% if user_signed_in? then %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Carga de Novedades <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <% if ver_model("AltasBajasHora") then %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Horas</a>
                      <ul class="dropdown-menu">
                        <li><%=link_to "Altas", altas_bajas_horas_path, :tabindex => "-1"%></li>
                        <li><%=link_to "Bajas", altas_bajas_horas_index_bajas_path, :tabindex => "-1"%></li>
                        <li><%=link_to "Asistencias", asistencia_path, :tabindex => "-1"%></li>  
                        <li><%=link_to "Novedades", horas_index_novedades_path, :tabindex => "-1"%></li>  
                        <li><%=link_to "Impresiones", lote_impresions_path, :tabindex => "-1"%></li>  
                      </ul>
                    </li>
                  <% end %>
                  <% if ver_model("AltasBajasCargo") then %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Cargos</a>
                      <ul class="dropdown-menu">
                        <li><%=link_to "Altas", altas_bajas_horas_path, :tabindex => "-1"%></li>
                        <li><%=link_to "Bajas", altas_bajas_horas_index_bajas_path, :tabindex => "-1"%></li>
                        <li><%=link_to "Asistencias", asistencia_path, :tabindex => "-1"%></li>  
                      </ul>
                    </li>
                  <% end %>
                </ul>
              </li>
          <% end %>
        </ul>

        <ul class="nav navbar-nav">
          <% if user_signed_in? then %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Información <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <% if ver_model("Establecimiento") then %>
                    <li><%=link_to "Establecimientos", establecimientos_path, :tabindex => "-1"%></li>
                  <% end %>
                  <% if ver_model("Persona") then %>
                    <li><%=link_to "Personas", personas_path, :tabindex => "-1"%></li>
                  <% end %>
                </ul>
              </li>
          <% end %>
        </ul>

        <ul class="nav navbar-nav">
          <% if user_signed_in? then %>
            <% if (current_user.role? :sadmin) then %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Importar Liquidacion <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li><%=link_to "Importar AltasBajasHoras", altas_bajas_horas_importar_path, :tabindex => "-1"%></li>
                    <li><%=link_to "Usuarios", users_path, :tabindex => "-1"%></li>
                    <li><%=link_to "Roles", roles_path, :tabindex => "-1"%></li>
                </ul>
              </li>
            <% end %>
          <% end %>
        </ul>

        <ul class="nav navbar-nav pull-right">
          <li class="dropdown">
            <% if user_signed_in? %>
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Bienvenido: <%= current_user.email %><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%=link_to "Mi cuenta", edit_user_registration_path, :tabindex => "-1"%></li>
                <li class="divider"></li>
                <li><%= link_to 'Salir', destroy_user_session_path, :method => :delete, :tabindex => "-1" %></li>        
              </ul>
            <% else %>
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">No ha iniciado sesión<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Iniciar Sesión", new_user_session_path , :tabindex => "-1" %></li> 
                <li class="divider"></li>
                <li><%= link_to "Registrar", new_user_registration_path, :tabindex => "-1"%></li> 
              </ul>        
            <% end %>
          </li>
        </ul>

      </div>
    </div>
  </nav>
  <!-- BODY 1-->
  <div class="container" id="contenedor_principal">
    <div class="row">
      <% if user_signed_in? then %>
        <% if session[:establecimiento] != nil then%>
          <%@e = Establecimiento.find(session[:establecimiento])%>                  
          <!-- SIDEBAR -->
          <div class="col-md-2">
            <div class="profile-sidebar">
              <!-- SIDEBAR USERPIC -->
              <div class="profile-userpic">
                  <%= image_tag("establecimientos/"+@e.cue_anexo.to_s+".jpg", :class => "img-circle img-responsive") %>
                  <!--<img src="imagenotfound.gif" alt="Image not found" onError="this.src='imagefound.gif';" />-->
              </div>
              <!-- END SIDEBAR USERPIC -->
              <!-- SIDEBAR USER TITLE -->
              <div class="profile-usertitle">
                  <div
                    class="profile-usertitle-name">
                    <%=current_user.nombres %>
                    <%=current_user.apellidos %>
                  </div>
                  
                    <div class="profile-usertitle-job text-center">
                      Escuela Nº: <%= @e.codigo_jurisdiccional %> </br>
                      <%= @e.nombre.titleize %></br>
                      <%= @e.localidad.to_s.titleize %>
                    </div>
                  
              </div>
              <!-- END SIDEBAR USER TITLE -->
            </div>
          </div>
          <!-- SIDEBAR END -->
        <% end %>          
      <% end %>
      <!--  -->
      <% if user_signed_in? then %>
        <div class="col-md-9">
      <% else %>
        <div class="col-md-12">
      <% end %>
          <div class="col-md-12 logo">
              <p align="center">
                <%=link_to  image_tag("logo.png"), "http://www.chubut.edu.ar"%>
              </p>
          </div>
          <div class="title">
            <h1 align="center"> Sistema de Novedades del Personal Educativo </h1>
          </div>
            
        </div>
        <!--  -->
  </div>
  <p align="center"></p>
    <% flash.each do |name, msg| %>
      <%if name.to_s=="info"%>
        <div class="alert alert-info">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        </div>
      <%elsif name.to_s=="danger" || name.to_s=="alert"%>
        <div class="alert alert-danger">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        </div>
      <%elsif name.to_s=="warning"%>
        <div class="alert alert-warning">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        </div>
      <%elsif name.to_s=="success" || name.to_s=="notice"%>
        <div class="alert alert-success">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        </div>
      <%else%>
        <div class="alert alert-danger">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        </div>
      <% end %>
    <% end %>
    <%= yield %>
  </div>
  <div class="footer navbar-fixed-bottom navbar-default text-center">
    <div class="footer-text">Sistema desarrollado por  <%=link_to  image_tag("logo_pregase.png"),"mailto:pregase.chubut@gmail.com"%>  - Ministerio de Educación de la Provincia del Chubut</p>
  </div>
</body>

</html>


<style type="text/css">
  body { padding-top: 60px; }
</style>
