<%= form_for(@funcion, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @funcion.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4>No se pudo guardar el formulario, tiene los siguientes errores:</h4>

      <ul>
      <% @funcion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :categoria, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :categoria, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :tipo_cargo, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :tipo_cargo, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :descripcion, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :descripcion, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :grupo, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :grupo, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">

      <%= f.submit :class => "btn btn-primary", :value => "Guardar" %>
      <%= link_to funcions_path, :class => 'btn btn-default' do %>
        <span class="glyphicon glyphicon-list-alt"></span>
        Atrás
      <% end %>
    </div>
  </div>
<% end %>
