<div class="page-header">
  <%= link_to new_grupo_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Grupo
  <% end %>
  <h1>Listado de grupos</h1>
</div>

<div class="table-responsive">
  <table id="tabla-grupo" class="table table-striped table-bordered table-hover" data-source = "<%= grupos_path(format: :json) %>" >
    <thead>
      <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th class="text-center col-md-1">Acciones</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>


<script type="text/javascript">
    $(document).ready(function($) {
      $('#tabla-grupo').dataTable({
        language : {"sUrl": "<%= asset_path('language.es.json') %>",},
        processing : true,
        serverSide : true,
        ajax : $('#tabla-grupo').data('source'),
        pagingType: "simple",
      });
    });
</script>