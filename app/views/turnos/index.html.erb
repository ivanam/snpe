<div class="page-header">
  <%= link_to new_turno_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Nuevo Turno
  <% end %>
  <h1>Listado de turnos</h1>
</div>

<div class="table-responsive">
  <table id="tabla-turno" class="table table-striped table-bordered table-hover" data-source = "<%= turnos_path(format: :json) %>" >
    <thead>
      <tr>
            <th>Descripción</th>
            <th class="text-center col-md-1">Acciones</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>


<script type="text/javascript">
    $(document).ready(function($) {
      $('#tabla-turno').dataTable({
        language : {"sUrl": "<%= asset_path('language.es.json') %>",},
        processing : true,
        serverSide : true,
        ajax : $('#tabla-turno').data('source'),
        pagingType: "simple",
      });
    });
</script>